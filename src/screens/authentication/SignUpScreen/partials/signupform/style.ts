import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        padding: 10,
        rowGap: 3,
    },
    buttonStyle: {
        padding: 10,
    },
    isInvalid: {
        borderWidth: 2,
    },
    valid: {
        borderWidth: 2,
    },
    error: {
        fontSize: 16,
    },
    innercon: {
        flexDirection: "row",
        alignItems: "center",

    }

})