import { StyleSheet } from "react-native";
export const styles = StyleSheet.create({
  GenresContainer: {
    flexDirection: "row"
  },
  GenreName: {
    padding: 3,
    fontSize: 16
  },
  GenreIndicator: {
    fontWeight: "bold",
    fontSize: 20
  }
});
